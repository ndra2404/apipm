const datetime = require('./datetime');
const string = require('./string');
const template = require('./template');
const response = require('./response');

module.exports = { datetime, string, template, response };
