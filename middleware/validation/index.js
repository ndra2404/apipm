const leave = require('./leave');
const medical = require('./medical');
const token = require('./token');
const notification = require('./notification');

module.exports = { leave, medical, token, notification };
